fix: 修复试炼之地装备显示问题并优化属性展示

1. 修复装备图标不显示问题
   - 兼容 _IconID 和 IconID 两种字段格式

2. 优化装备属性显示逻辑
   - 优先使用 _Magic1Type 到 _Magic16Type 字段获取绿字属性
   - 兼容旧格式 attributes 数组（仅当新格式无数据时）
   - 属性显示数量从 6 个调整为 7 个
   - 移除"全能"和"特效"过滤条件

3. 数据库迁移 V11：清理重复装备数据
   - 删除同时存在新旧格式的旧格式装备
   - 保留无对应新格式的旧格式装备（支持历史记录）

4. 前端兼容旧 ID 查找
   - 支持旧格式 ID（如 6_42783）匹配新格式装备

5. UI 优化
   - 统一挑战层数输入框与关卡 BOSS 选择器高度
